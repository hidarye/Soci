# مرجع سريع - السجلات (Quick Reference)

## السؤال
> الكونسول لا يظهر فيها سجلات؟

## الإجابة المختصرة
السجلات من Client Components (`'use client'`) تظهر في **Browser Console** (`F12`)، وليس في Terminal.

## الفروقات

```
┌─────────────────────────────────────────────────────────┐
│                    Browser Console                      │
│  Where: متصفح الويب                                    │
│  How: اضغط F12                                          │
│  What: سجلات من Client Components                      │
│  في الكود:                                             │
│  ├─ console.log('[v0] ...')                             │
│  ├─ useEffect hooks                                     │
│  └─ event handlers                                      │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│                    Server Console                       │
│  Where: Terminal (حيث تشغل npm run dev)               │
│  How: انظر إلى الـ Terminal                            │
│  What: سجلات من Server Components                      │
│  في الكود:                                             │
│  ├─ بدون 'use client'                                  │
│  ├─ Server Actions                                      │
│  └─ Route Handlers                                      │
└─────────────────────────────────────────────────────────┘
```

## خطوات سريعة لفتح Browser Console

### الجميع
```
اضغط: F12
اختر: Console tab
```

### المفاتيح حسب النظام
- **Windows:** `Ctrl + Shift + I`
- **Mac:** `Cmd + Option + I`
- **Linux:** `Ctrl + Shift + I`

## اختبار سريع

1. افتح التطبيق
2. اضغط `F12`
3. اختر `Console`
4. قم بأي إجراء (انقر على زر، أملأ نموذج، إلخ)
5. **ستجد السجلات تظهر فوراً!**

## السجلات الحالية في التطبيق

جميع السجلات تبدأ بـ `[v0]` لسهولة البحث.

### في لوحة التحكم
```
[v0] Dashboard: Component mounted
[v0] Dashboard: Found users: 1
[v0] Dashboard: Dashboard data loaded successfully
```

### في صفحة المهام
```
[v0] TasksPage: Component mounted
[v0] TasksPage: Tasks loaded: 2
```

### في إنشاء مهمة
```
[v0] CreateTaskPage: Component mounted
[v0] handleSubmit: Form submitted
[v0] handleSubmit: Task created successfully: task-123
```

## البحث في السجلات

في Console، اكتب:
```javascript
// ابحث عن جميع السجلات الخاصة بنا
console.log('%c[v0]', 'color: blue', 'البحث عن هذا');
```

أو استخدم مربع البحث في Console وابحث عن `[v0]`

## إذا لم تر سجلات

1. ✅ تأكد من أنك في صفحة Client Component (`'use client'` في الأعلى)
2. ✅ حدّث الصفحة: `F5`
3. ✅ امسح الـ Cache: `Ctrl + Shift + Delete`
4. ✅ استخدم متصفح مختلف
5. ✅ اقرأ `/DEBUGGING_GUIDE.md` للمزيد من التفاصيل

## الملفات المرتبطة

- 📄 `DEBUGGING_GUIDE.md` - دليل شامل
- 📄 `CONSOLE_LOGS_GUIDE.md` - شرح تفصيلي
- 📄 `CONSOLE_ISSUE_RESOLVED.md` - حل المشكلة

---

**باختصار:** 
```
صفحة مفتوحة → اضغط F12 → اختر Console → قم بإجراء → شاهد السجل!
```

✅ انتهى!
